# medical-clustering-analysis
Comparison of clustering algorithms (K-means, K-medoids and hierarchical clustering) applied to a medical dataset. The project includes data preprocessing, implementation of methods, and evaluation of cluster structures using Python and Jupyter Notebook.
